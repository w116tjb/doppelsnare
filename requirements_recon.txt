# DoppelSnare + DoppelSnare Recon — Python dependencies
#
# Install with:
#   pip install -r requirements.txt
#
# For screenshots (optional):
#   pip install playwright && playwright install chromium

# ── Required by both scripts ─────────────────────────────────────────────────
dnspython>=2.4.0
requests>=2.31.0

# ── Used by doppelsnare.py (WHOIS fallback) ──────────────────────────────────
python-whois>=0.9.0

# ── Used by doppelsnare_recon.py (cert parsing for invalid certs) ────────────
cryptography>=41.0.0

# ── Optional: screenshots in doppelsnare_recon.py ────────────────────────────
# playwright>=1.40.0
